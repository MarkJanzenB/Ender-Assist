<UserControl x:Class="MarlinPrintMiddleware.UI.Views.Controls.TerminalSectionView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ctrl="clr-namespace:MarlinPrintMiddleware.UI.Views.Controls">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <ctrl:ConsolePanelView Grid.Column="0" Margin="0,0,8,0"/>
        <ctrl:ControlPanelView Grid.Column="1" Margin="8,0,0,0"/>
    </Grid>
</UserControl>
